service: auth-server

provider:
  name: aws
  runtime: nodejs12.x
  region: eu-central-1
  timeout: 60 # optional, in seconds, default is 6
  environment:
    CLIENT_ID: ${file(config.json):"694465006279-3t9oap9tn3tqol23om76gr4qdolb3k77.apps.googleusercontent.com"}
    CLIENT_SECRET: ${file(config.json):"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"}
    PROJECT_ID: ${file(config.json):"meet-app-337400"}
    CALENDAR_ID: ${file(config.json):"fullstackwebdev@careerfoundry.com"}
functions:
  getAuthURL:
    handler: handler.getAuthURL
    events:
      - http:
          path: api/get-auth-url
          method: get
  getAccessToken:
    handler: handler.getAccessToken
    events:
      - http:
          path: api/token/{code}
          method: get